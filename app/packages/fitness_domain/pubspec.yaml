name: fitness_domain
description: "Domain layer — domain models, repository interfaces, business rules."
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0
  fitness_data:
    path: ../fitness_data

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.12.2
  freezed: ^3.2.3
  json_serializable: ^6.11.2
